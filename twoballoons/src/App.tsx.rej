diff a/twoballoons/src/App.tsx b/twoballoons/src/App.tsx	(rejected hunks)
@@ -5,11 +5,37 @@ import { LeftToolbar } from "./components/LeftToolbar";
 import { ConsoleHUD } from "./components/ConsoleHUD";
 import { VaultExplorer } from "./components/VaultExplorer";
 import { DiagramModal } from "./components/DiagramModal";
+import { ExportModal } from "./components/ExportModal";
+import { PeerCursors } from "./components/PeerCursors";
+import { SearchModal } from "./components/SearchModal";
 import { useStore } from "./store";
+import { initSync, initAwareness, updateCursor } from "./sync/crdtProvider";
+import { useEffect } from "react";
 import "./App.css";
 
 function App() {
-  const { language, isDiagramModalOpen, setIsDiagramModalOpen } = useStore();
+  const { language, isDiagramModalOpen, setIsDiagramModalOpen, isExportModalOpen, setIsExportModalOpen, isSearchModalOpen, setIsSearchModalOpen } = useStore();
+
+  useEffect(() => {
+    const cleanupSync = initSync();
+    initAwareness({ name: "User-" + Math.floor(Math.random() * 1000), color: "#c2652a" });
+
+    let animationFrameId: number;
+    const handleMouseMove = (e: MouseEvent) => {
+      cancelAnimationFrame(animationFrameId);
+      animationFrameId = requestAnimationFrame(() => {
+        updateCursor({ x: e.clientX, y: e.clientY });
+      });
+    };
+
+    window.addEventListener("mousemove", handleMouseMove);
+
+    return () => {
+      window.removeEventListener("mousemove", handleMouseMove);
+      cancelAnimationFrame(animationFrameId);
+      cleanupSync();
+    };
+  }, []);
 
   return (
     <main className="canvas-bg text-[#3a302a] font-sans h-screen w-screen overflow-hidden relative selection:bg-[#c2652a]/30 selection:text-[#c2652a]">
@@ -43,12 +69,27 @@ function App() {
       {/* 5. Floating Console HUD */}
       <ConsoleHUD />
 
+      {/* Peer Cursors */}
+      <PeerCursors />
+
       {/* 6. Universal Transpiler Modal */}
       <DiagramModal 
         isOpen={isDiagramModalOpen} 
         onClose={() => setIsDiagramModalOpen(false)} 
       />
 
+      {/* 6.5 Export Modal */}
+      <ExportModal
+        isOpen={isExportModalOpen}
+        onClose={() => setIsExportModalOpen(false)}
+      />
+
+      {/* 6.75 Search Modal */}
+      <SearchModal
+        isOpen={isSearchModalOpen}
+        onClose={() => setIsSearchModalOpen(false)}
+      />
+
       {/* 7. Footer Status Bar */}
       <footer className="absolute bottom-0 w-full z-50 flex items-center justify-between px-4 h-8 bg-[#f2ece4]/90 backdrop-blur border-t border-[#d8d0c8] pointer-events-auto">
         <div className="text-[10px] uppercase tracking-wider text-[#c2652a] font-bold">
